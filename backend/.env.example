# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

POSTGRES_PASSWORD=mypassword
POSTGRES_USER=myuser
POSTGRES_DB=mydatabase
POSTGRES_PORT=5433
POSTGRES_ADDRESS=localhost
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_ADDRESS}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

JWT_SECRET=secret
TOKEN_COOKIE_NAME=token
REFRESH_TOKEN_COOKIE_NAME=refresh_token

CORS_ORIGIN=http://localhost:5173

EMAIL=fireSpreadPA@gmail.com
EMAIL_PASSWORD=fireSpreadPA123
EMAIL_REFRESH_TOKEN=refresh
EMAIL_REDIRECT_URI=https://developers.google.com/oauthplayground

HTTPS_KEY=./certs/localhost-key.pem
HTTPS_CERT=./certs/localhost.pem